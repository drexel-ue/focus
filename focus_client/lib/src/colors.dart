import 'dart:ui';

/// Focus theme colors.
class AppColors {
  const AppColors._();

  /// Black.
  static const black = Color(0xFF000000);

  /// White.
  static const white = Color(0xFFFFFFFF);

  /// Soft Blue.
  static const softBlue = Color(0xFFADD8E6);

  /// Dull Brown.
  static const dullBrown = Color(0xFF795947);

  /// Red-Orange.
  static const redOrange = Color(0xFFFF681F);

  /// Navy Blue.
  static const navyBlue = Color(0xFF000080);

  /// Deep Purple.
  static const deepPurple = Color(0xFF570861);

  /// Vibrant Red.
  static const vibrantRed = Color(0xFFFF272A);

  /// Dull Gray.
  static const dullGray = Color(0xFF8B837E);

  /// Drab.
  static const drab = Color(0xFF967117);

  /// Dark Olive Green.
  static const darkOliveGreen = Color(0xFF556B2F);

  /// Icy Blue.
  static const icyBlue = Color(0xFF97C1E6);
}
