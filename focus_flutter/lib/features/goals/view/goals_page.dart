import 'package:flutter/widgets.dart';

/// Goals Page.
@immutable
class GoalsPage extends StatelessWidget {
  /// Construcs a const [GoalsPage].
  const GoalsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Goals'));
  }
}
