import 'package:flutter/widgets.dart';

/// Games Page.
@immutable
class GamesPage extends StatelessWidget {
  /// Construcs a const [GamesPage].
  const GamesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Games'));
  }
}
