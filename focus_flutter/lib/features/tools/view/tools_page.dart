import 'package:flutter/widgets.dart';

/// Tools Page.
@immutable
class ToolsPage extends StatelessWidget {
  /// Construcs a const [ToolsPage].
  const ToolsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Tools'));
  }
}
